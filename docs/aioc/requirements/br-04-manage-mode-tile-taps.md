# BR-4 — Manage mode tile taps

**Status:** Not reachable — the app lists no profile but the one in session, so there is no other profile to edit and nothing to refuse

## The requirement

> Tapping a tile opens that profile's Manage Profile screen if it is the active profile, or
> if the active profile is the owner. Otherwise an alert says only the account owner can
> manage other profiles.
